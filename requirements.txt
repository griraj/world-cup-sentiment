# ── Core ──────────────────────────────────────────
dash>=2.17.0
plotly>=5.22.0
pandas>=2.2.0
numpy>=1.26.0

# ── NLP ───────────────────────────────────────────
transformers>=4.41.0
torch>=2.2.0                 # CPU build; for GPU use torchvision wheel
tokenizers>=0.19.0
sentencepiece>=0.2.0

# ── Twitter / Streaming ───────────────────────────
tweepy>=4.14.0

# ── Database ──────────────────────────────────────
sqlalchemy>=2.0.30

# ── Utilities ─────────────────────────────────────
python-dotenv>=1.0.1
requests>=2.31.0

# ── Optional: PostgreSQL support ──────────────────
# psycopg2-binary>=2.9.9

# ── Optional: production WSGI ─────────────────────
# gunicorn>=22.0.0

# ── Optional: Kafka streaming (bonus) ─────────────
# confluent-kafka>=2.4.0

# ── Optional: Redis caching (bonus) ───────────────
# redis>=5.0.0
